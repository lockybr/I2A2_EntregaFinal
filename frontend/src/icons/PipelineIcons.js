// PipelineIcons removed — icons were only used by the legacy detail/result view
// which has been permanently removed. Keeping an empty default export so
// any stale imports won't cause build failures. If you want to reintroduce
// icons, reimplement the components here.

const EMPTY = {};
export default EMPTY;
